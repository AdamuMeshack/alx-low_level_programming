#include "main.h"

/**
 * _isuppeir - check if character is uppercase or not
 * @c character yo be test
 * Return: 1 whether it is 0, otherwise
 */

int _isupper(int c);
{
	if (c >= 65 && c <= 90)
	{
		return (1);
	}
	else
	{
		return (0);
	}
}
